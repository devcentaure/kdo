{% extends '::base.html.twig' %}

{% block title %}
    {{ 'app_kdo.index.title'|trans }}
{% endblock title %}

{% block page %}
    <div class="row">
        <div class="small-10 columns">
            <h1 class="text-center title">Kdo list</h1>
        </div>
                <div class="small-2 columns">
                <a class="tiny button" href="{{ path('app_kdo_new') }}">
                    {{ 'button.create'|trans }}
                </a>
            </div>
        </div>


    {{ grid(grid) }}

{% endblock %}
