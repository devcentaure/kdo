{% extends '::base.html.twig' %}


{% block title %}
    {{ 'appbundle_userkdo.index.title'|trans }}
{% endblock title %}

{% block page %}
    <div class="row">
        <div class="medium-12 columns">
            <h1 class="text-center title">Entity</h1>
        </div>
    </div>
    <div class="row">
        <div class="medium-12 columns">
            <table class="width-100 panel">
                <tbody>
                    <tr>
                        <th>Id</th>
                            <td>{{ entity.id }}</td>
                    </tr>
                    <tr>
                        <th>Createdat</th>
                            <td>{{ entity.createdAt|date('Y-m-d H:i:s') }}</td>
                    </tr>
                    <tr>
                        <th>Usershare</th>
                            <td>{{ entity.userShare }}</td>
                    </tr>
                </tbody>
            </table>

                        <div class="row">
    <div class="small-4 columns text-left">
        <a class="link-return" href="{{ path('appbundle_userkdo') }}">
            <i class="icon-arrow-left icon-large vertical"></i>
            {{ 'link.return'|trans }}
        </a>
    </div>
    <div class="small-4 columns text-center">
        <a class="tiny button" href="{{ path('appbundle_userkdo_edit', { 'id': entity.id }) }}">
            {{ 'button.update'|trans }}
        </a>
    </div>
    <div class="small-4 columns text-center">
        {{ form(delete_form) }}
    </div>
</div>
        </div>
    </div>
{% endblock %}
